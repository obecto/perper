function FilterUtils () {
  // Hello World!
}

FilterUtils.parse_field_name = function (subexpression) {};
FilterUtils.parse_field_value = function (subexpression) {};
FilterUtils.add_to_filter = function (filter, subexpression) {};

FilterUtils.prototype.convert_filter = function (cls, filter) {
  return filter;
};

module.exports = FilterUtils;
